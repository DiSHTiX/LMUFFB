Please proceed with the following task:

**Task: Implement Advanced Grip Estimation Settings & Steering Shaft Smoothing**

**Reference Documents:**
*   `docs\dev_docs\Optimal Slip Angle and steering shaft torque smooting sliders implementation plan.md` (Contains the detailed technical specification and code snippets).
*   `FFBEngine.h`
*   `src/Config.h` / `src/Config.cpp`
*   `src/GuiLayer.cpp`

**Context:**
We are enhancing the physics customization to support the diverse vehicle classes in Le Mans Ultimate (e.g., GT3 vs. Hypercars).
1.  **Grip Estimation:** Currently, the "Optimal Slip Angle" (peak grip) is hardcoded to 0.10 rad. We need to make this configurable so users can tune it for stiff prototype tires (which peak earlier, e.g., 0.06 rad) vs. softer GT tires.
2.  **Signal Quality:** Users report "graininess" in the raw game force. We need a dedicated smoothing filter for the `mSteeringShaftTorque` input before it is processed by other effects.

**Implementation Requirements:**

1.  **Physics Engine (`FFBEngine.h`)**:
    *   Add member variables: `m_optimal_slip_angle` (default 0.10), `m_optimal_slip_ratio` (default 0.12), and `m_steering_shaft_smoothing` (default 0.0).
    *   **Steering Shaft Smoothing:** Implement a Time-Corrected Low Pass Filter (LPF) for `mSteeringShaftTorque` at the very beginning of `calculate_force`.
    *   **Grip Calculation:** Update `calculate_grip` to use the new member variables instead of hardcoded constants (0.10 and 0.12) for the friction circle normalization.
    *   **Documentation:** Add the required comment explaining why we use `mSteeringShaftTorque` instead of `mSteeringArmForce` (see Reference Document).

2.  **Configuration (`src/Config.h` & `src/Config.cpp`)**:
    *   Update `Preset` struct to include the 3 new fields.
    *   Implement fluent setters (`SetOptimalSlip`, `SetShaftSmoothing`).
    *   Update `Save` and `Load` logic to persist these settings to `config.ini`.

3.  **GUI Layer (`src/GuiLayer.cpp`)**:
    *   **Front Axle Section:** Add the "Steering Shaft Smooth" slider with the Latency Display logic (Red/Green text) similar to the existing smoothing sliders.
    *   **Grip & Slip Section:** Add sliders for "Optimal Slip Angle" (0.05 - 0.20 rad) and "Optimal Slip Ratio" (0.05 - 0.20). Include the tooltips defined in the Reference Document.

4.  **Comprehensive Testing (`tests/test_ffb_engine.cpp`)**:
    *   **Requirement:** You must implement robust unit tests to verify these features work as intended.
    *   **Test 1: Grip Threshold Sensitivity:**
        *   Set up a scenario with a fixed Slip Angle (e.g., 0.08 rad).
        *   Set `m_optimal_slip_angle` to 0.10 -> Verify Grip is High (Not sliding).
        *   Set `m_optimal_slip_angle` to 0.06 -> Verify Grip drops (Understeer detected).
    *   **Test 2: Steering Shaft Smoothing:**
        *   Inject a step change in `mSteeringShaftTorque` (0 -> 10 Nm).
        *   Verify that with `smoothing = 0.0`, output is instant.
        *   Verify that with `smoothing = 0.1`, output lags/ramps up over multiple frames.

**Deliverables:**
*   Modified `FFBEngine.h`
*   Modified `src/Config.h` and `src/Config.cpp`
*   Modified `src/GuiLayer.cpp`
*   Modified `tests/test_ffb_engine.cpp` (New tests added)
*   Updated `CHANGELOG.md` and `VERSION`

**Check-list for completion:**
- [ ] `FFBEngine.h` updated with new variables and logic.
- [ ] `calculate_grip` uses dynamic thresholds.
- [ ] `mSteeringShaftTorque` is smoothed before use.
- [ ] Config system saves/loads new keys.
- [ ] GUI displays new sliders with correct ranges and tooltips.
- [ ] Latency display implemented for Shaft Smoothing slider.
- [ ] Unit tests implemented and **PASSED**.
- [ ] Documentation updated.