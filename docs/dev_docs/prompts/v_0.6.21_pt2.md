Please proceed with the following task:

**Task: Implement Telemetry Diagnostics for Missing Vertical Tire Deflection**

**Reference Documents:**
*   `docs\dev_docs\implement the missing mVerticalTireDeflection check .md` (Contains the specific code snippets for the warning logic and tests)
*   `src/FFBEngine.h`
*   `tests/test_ffb_engine.cpp`

**Context:**
We need to improve diagnostics by explicitly detecting and warning the user if `mVerticalTireDeflection` is missing (blocked/zeroed), which occurs on encrypted DLC cars. This helps users understand why effects like Road Texture might be silent or using fallback logic.

**Implementation Requirements:**

1.  **Modify `src/FFBEngine.h`**:
    *   **Add Member Variables**: 
        *   `bool m_warned_vert_deflection = false;`
        *   `int m_missing_vert_deflection_frames = 0;`
    *   **Implement Detection Logic**:
        *   Inside `calculate_force`, check if `mVerticalTireDeflection` (average of front wheels) is exactly `0.0` (or `< 0.000001`) while the car is moving fast (`speed > 10.0 m/s`).
        *   Implement a hysteresis counter (trigger warning only after 50 consecutive frames of missing data).
        *   **Console Output**: If triggered, print: `"[WARNING] mVerticalTireDeflection is missing for car: ... (Likely Encrypted/DLC Content). Road Texture fallback active."`

2.  **Modify `tests/test_ffb_engine.cpp`**:
    *   **Update `test_missing_telemetry_warnings()`**: 
        *   Add a new test case "Case 3: Missing Vertical Tire Deflection".
        *   Simulate a condition with `mVerticalTireDeflection = 0.0` and `Speed = 20.0`.
        *   Run the engine for > 50 frames.
        *   Capture `std::cout` and verify the specific warning message appears.

3.  **Documentation**:
    *   Update `CHANGELOG.md` with a new version entry.
    *   Increment `VERSION`.

**Deliverables:**
*   Modified `src/FFBEngine.h`
*   Modified `tests/test_ffb_engine.cpp`
*   Updated `CHANGELOG.md` and `VERSION`

**Check-list for completion:**
- [ ] `FFBEngine.h`: Warning state variables added.
- [ ] `FFBEngine.h`: Detection logic and console print implemented.
- [ ] `test_ffb_engine.cpp`: Test case added to `test_missing_telemetry_warnings`.
- [ ] `CHANGELOG.md` and `VERSION` updated.
- [ ] Code compiles and tests pass.