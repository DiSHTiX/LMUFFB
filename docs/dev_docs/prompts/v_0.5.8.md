Please proceed with the following task:

**Task: Improve FFB Error Handling & Default Window Behavior**

**Reference Documents:**
*   `docs\dev_docs\improve no FFB error handling if not on top.md` (Contains the detailed analysis and code snippets)
*   `src\DirectInputFFB.cpp`
*   `src\Config.cpp`
*   `tests\test_ffb_engine.cpp`

**Context:**
Users have reported that Force Feedback stops working if the LMUFFB window is not focused ("on top"). The logs indicate an "Unknown" DirectInput error occurs when the app is in the background, but the current logic does not attempt to recover from unknown errors. Additionally, to prevent Windows from deprioritizing the app, we want to enable "Always on Top" by default.

**Implementation Requirements:**

1.  **Update `src/DirectInputFFB.cpp` (Aggressive Recovery):**
    *   Include `<iomanip>` for hex formatting.
    *   In `UpdateForce`, update the error logging to print the HRESULT error code in hexadecimal (e.g., `0x80070005`).
    *   Change the recovery logic: Default `recoverable` to `true` for *all* failed `SetParameters` calls (including "Unknown" errors).
    *   Ensure that when recovery succeeds (`Acquire` returns success), we explicitly call `m_pEffect->Start(1, 0)` to restart the FFB motor.

2.  **Update `src/Config.cpp` (Defaults):**
    *   Change the initialization of `Config::m_always_on_top` from `false` to `true`.

3.  **Update `tests/test_ffb_engine.cpp` (Regression Test):**
    *   Add a new test function `test_config_defaults_v057()`.
    *   Assert that `Config::m_always_on_top` is `true`.
    *   Call this test from `main()`.

4.  **Documentation & Versioning:**
    *   Update `VERSION` to `0.5.7`.
    *   Update `CHANGELOG.md` with the changes regarding connection recovery and default settings.

**Deliverables:**
*   Modified `src/DirectInputFFB.cpp`
*   Modified `src/Config.cpp`
*   Modified `tests/test_ffb_engine.cpp`
*   Updated `VERSION` and `CHANGELOG.md`

**Check-list for completion:**
- [ ] `DirectInputFFB.cpp`: Error logging includes hex code.
- [ ] `DirectInputFFB.cpp`: Unknown errors trigger recovery attempt.
- [ ] `DirectInputFFB.cpp`: Recovery includes `m_pEffect->Start(1, 0)`.
- [ ] `Config.cpp`: `m_always_on_top` defaults to true.
- [ ] `test_ffb_engine.cpp`: New test verifies default config.
- [ ] Version bumped to 0.5.7.